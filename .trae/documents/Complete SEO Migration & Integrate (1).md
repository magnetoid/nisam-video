**1**: Edit 0003.sql full; RunCommand `npx drizzle-kit push:pg`.

**2**: schema.ts +relations/Zod; database.ts +CRUD (get/upsert/delete, seoScore).

**3**: seo.ts typed/paginated; SEO.tsx dynamic fetch/apply.

**4**: typecheck, diagnostics, curl, browser test.
